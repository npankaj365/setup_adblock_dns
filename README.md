# Script to setup Adblock DNS
This script will modify the dns to adblock servers, so that you don't have to deal with ads anymore. You can test it by going to a site that serves ads, set this up and then visit the site again. 


## For Mac OS
```bash
chmod +x install.sh
sudo ./install.sh
```


